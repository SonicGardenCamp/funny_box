<% provide(:button_text, '作成') %>



<div class="card mb-3">
  <div class="card-header">
    グループ作成
  </div>
  <div class="card-body">
    <%= render 'form' %>
  </div>
</div>

<%= link_to "一覧へ戻る", groups_path %>