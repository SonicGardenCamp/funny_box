<h1>ログイン</h1>

<div>
  <div>
    <%= form_with(url: login_path, scope: :session) do |f| %>

      <%= f.label :email %>
      <%= f.email_field :email %>

      <%= f.label :password %>
      <%= f.password_field :password %>

      <%= f.submit "ログイン" %>
    <% end %>

    <p>ユーザー登録をしていない方は<%= link_to "ユーザー登録", signup_path %>をしてください</p>
  </div>
</div>
