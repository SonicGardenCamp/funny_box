<% if object.errors.any? %>
  <div>
    <div>
      <%= object.errors.count %>件のエラーがあります。
    </div>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>